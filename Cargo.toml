[package]
name = "omnitop"
version = "0.1.0"
edition = "2021"
description = "A unified system monitor: processes, containers, GPU, and network in one TUI"
license = "MIT"

[dependencies]
ratatui = "0.29"
crossterm = "0.28"
sysinfo = "0.33"

[profile.release]
lto = true
strip = true
